<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', { title: 'Login' }) %>

    <body>

        <%- include('partials/header') %>

            <main>

                <!-- CABECERA  -->

                <h3 class="title-login">Iniciar sesión</h3>

                <!-- FORMULARIO -->

                <div class="container_main-login">
                    <section class="formulario-register">
                        <div class="inputs">
                            <p>Los campos con (*) son obligatorios</p>
                            <form class="formu" method="POST">
                                <div class="espacio">
                                    <label for="email">E-mail<p>*</p></label>
                                    <input class="form-control"type="email" id="email" name="email" value="<%= locals.old?.email %>" <%= locals.errors && (errors.email && 'is-invalid') %>/>
                                    <small class="text-danger ms-2"><%= locals.errors && (errors.email && errors.email.msg) %></small>

                                </div>
                                <div class="espacio">
                                    <label for="password">Contraseña<p>*</p></label>
                                    <div class="input_login">
                                        <input class="form-control" type="password" id="password" name="password">
                                        <i class="fa-solid fa-eye"></i>
                                    </div>
                                </div>
                                <div class="col-12 mb-3">
                                <small class="text-danger ms-2"><%= locals.errors?.password?.msg %></small>
                                 </div>
                                <div class="d-flex justify-content-around">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" value="" id="remember" name="remember">
                                        <label class="form-check-label" for="remember"> 
                                            Recordarme
                                        </label>
                                    </div>

                                </div>
                            
                        </div>
                        <p><a href="/users/register" class="boton_formulario">¿Olvidaste tu contraseña?</a></p>
                        <button type="submit" class="btn-submit"><span class="link-register">INICIAR
                                SESION</span></button>
                    </section>
                </form>
                    <div class="formulario-register_register">
                        <h2>Registrarse</h2>
                        <p>Crear una cuenta tiene muchos beneficios que te ayudará con tu compra como para recibir
                            nuestras
                            grandes ofertas que tenemos para vos!!</p>
                        <button type="submit" class="btn-submit"><a href="/users/register" class="link-register">CREAR
                                CUENTA</a></button>
                    </div>
                </div>


            </main>

            <%- include('partials/footer') %>

    </body>

</html>